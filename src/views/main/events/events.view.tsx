import CreateEventDialog from "../modals/createEvent.modal";

const EventsView = () => {
  return (
    <div className="w-full">
      EventsView
      <CreateEventDialog />
    </div>
  );
};

export default EventsView;
